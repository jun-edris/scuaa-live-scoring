import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
	card: {
		position: 'relative',
		overflow: 'visible',
		marginBottom: '50px',
		width: '100%',
		height: 'auto',
	},
}));
