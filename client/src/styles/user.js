import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({
	root: {
		width: '100%',
	},
	wrapper: {
		width: '100%',
		minHeight: '100%',
		display: 'flex',
		flexWrap: 'wrap',
		alignItems: 'start',
		marginTop: '1rem',
	},
}));
